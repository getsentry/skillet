# ──────────────────────────────────────────────────────────
# Managed by skillet. Do NOT edit this file by hand.
# Use `skillet spec --help` to modify intent, behaviors, or
# triggers. Hand edits will be overwritten on the next regen.
# ──────────────────────────────────────────────────────────
# This fixture intentionally has coverage gaps to exercise verify's
# layer-2 (cross-artifact) checks.
managed_by: skillet
spec_version: 1
name: incomplete-skill
class: generic
intent: |
  A test fixture with an intentionally uncovered behavior and an
  orphan eval case, used to exercise verify's coverage layer.

triggers:
  should:
    - test verify failures

behaviors:
  - id: covered-behavior
    statement: This behavior has a matching eval case.
  - id: uncovered-behavior
    statement: This behavior has NO matching eval case (intentional).

must_not: []
