evals:
  - name: greets by name
    turns:
      - "Greet Alice"
    checks:
      - output_contains: "Hello"
      - output_contains: "Alice"

  - name: default greeting
    turns:
      - "Generate a greeting"
    checks:
      - output_contains: "Hello"
