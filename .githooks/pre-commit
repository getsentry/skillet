#!/bin/sh
# Canonical gate: typecheck + oxlint(+ast-grep) + format + tests.
# Installed via `npm install` (prepare -> core.hooksPath .githooks).
for name in $(git rev-parse --local-env-vars); do
  unset "$name"
done
npm run check
